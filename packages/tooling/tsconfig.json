{
  "extends": "./config/tsconfig.base.json",
  // Node runs these files directly, stripping their types.
  "compilerOptions": { "erasableSyntaxOnly": true },
  "include": ["src/**/*.ts", "config/**/*.ts", "vitest.config.ts"]
}
