{
  "name": "@rxova/homepage-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node ../scripts/sync-brand-og.mjs",
    "predev": "node ../scripts/sync-brand-og.mjs",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@rxova/brand": "^0.14.2",
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "6.0.3"
  }
}
