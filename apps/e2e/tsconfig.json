{
  "extends": "@rxova/repo-tooling/tsconfig.base.json",
  // Specs pass functions to the page, so they see the DOM too.
  "compilerOptions": { "lib": ["ES2024", "DOM"] },
  "include": ["e2e/**/*.ts", "playwright.config.ts"]
}
