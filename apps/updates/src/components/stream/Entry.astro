---
/** One update, rendered in full; its `#id` is the permalink. */
import type { UpdateEntry } from '../../lib/content'
import { repoLabel, formatDate, isoDate, byline, href } from '../../lib/content'

export interface StreamItem {
  entry: UpdateEntry
  Body: import('astro/runtime/server/index.js').AstroComponentFactory
  authorNames: string[]
}

type Props = StreamItem

const { entry, Body, authorNames } = Astro.props
---

<li
  class="entry"
  id={entry.id}
  data-repos={entry.data.repos.join(' ')}
  data-tags={entry.data.tags.join(' ')}
>
  <div class="when">
    <time datetime={isoDate(entry.data.date)}>{formatDate(entry.data.date)}</time>
  </div>
  <div class="what">
    <h2>
      <a href={`#${entry.id}`} class="anchor">
        {entry.data.title}
      </a>
    </h2>
    <p class="meta">
      {entry.data.draft && <span class="draft">Draft</span>}
      {entry.data.repos.map((r) => (
        <a class="repo" href={href(`repos/${r}`)}>
          {repoLabel(r)}
        </a>
      ))}
      {entry.data.tags.map((t) => (
        <span class="tag">{t}</span>
      ))}
      {entry.data.version && <span class="version">{entry.data.version}</span>}
    </p>
    <div class="prose">
      <Body />
    </div>
    {entry.data.links.length > 0 && (
      <nav class="links" aria-label={`${entry.data.title} links`}>
        {entry.data.links.map((l) => (
          <a href={l.href} target="_blank" rel="noopener noreferrer">
            {l.label} <span aria-hidden="true">↗</span>
          </a>
        ))}
      </nav>
    )}
    <p class="by">{byline(authorNames)}</p>
  </div>
</li>

<style>
  .entry {
    display: grid;
    gap: 0.35rem 2rem;
    /* Date beside the entry on wide screens, above it on narrow. */
    grid-template-columns: minmax(0, 1fr);
  }
  @media (min-width: 46rem) {
    .entry {
      grid-template-columns: 9rem minmax(0, 1fr);
    }
  }
  .when time {
    color: var(--rx-faint);
    font-size: 0.85rem;
    font-family: var(--rx-font-mono);
  }
  /* The entry's own measure: 68ch, the same as a blog post. */
  .what {
    max-width: 68ch;
  }
  .what h2 {
    font-size: 1.1rem;
    font-weight: 640;
    letter-spacing: -0.01em;
  }
  .anchor {
    color: var(--rx-fg);
    text-decoration: none;
  }
  .anchor:hover {
    text-decoration: underline;
    text-underline-offset: 4px;
  }
  .meta {
    display: flex;
    gap: 0.4rem;
    flex-wrap: wrap;
    margin: 0.4rem 0 0.75rem;
    font-size: 0.78rem;
  }
  .repo,
  .tag,
  .version {
    border-radius: 999px;
    padding: 0.1rem 0.55rem;
    background: var(--rx-tag-bg);
    color: var(--rx-muted);
  }
  .repo {
    text-decoration: none;
    border: 1px solid var(--rx-rule);
  }
  .repo:hover {
    color: var(--rx-fg);
  }
  .version {
    font-family: var(--rx-font-mono);
    background: none;
    color: var(--rx-faint);
    padding-left: 0;
  }
  /* Only rendered by `pnpm dev`: sketches are filtered out of the production build. */
  .draft {
    border-radius: 999px;
    padding: 0.1rem 0.55rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--rx-faint);
    border: 1px solid var(--rx-rule);
  }
  .links {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 0.9rem;
    font-size: 0.9rem;
  }
  .links a {
    color: var(--rx-muted);
    text-decoration: none;
  }
  .links a:hover {
    color: var(--rx-fg);
  }
  .by {
    margin-top: 0.9rem;
    color: var(--rx-faint);
    font-size: 0.82rem;
  }
  .entry[hidden] {
    display: none;
  }
  /* Batched away (`.beyond`, "not yet"), as opposed to filtered away (`hidden`).
     Print keeps the filtered state and drops the batching. */
  .entry.beyond {
    display: none;
  }

  /* An entry the script revealed and then focused. Without JS nothing is focused
     here, so this never fires on the initial page. */
  .entry:focus {
    outline: none;
  }
  /* Paper has no "show more". Print everything that passes the filters, batched
     or not — but still honour the filters themselves. */
  @media print {
    .entry.beyond {
      display: grid;
    }
    .entry[hidden] {
      display: none;
    }
  }
</style>
