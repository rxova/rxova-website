{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  // The walkthroughs' `after` modules have their own config, src/showcases/tsconfig.json,
  // since two are React components and this one types JSX as Astro's.
  "exclude": ["dist", "src/showcases/*/after.*"]
}
