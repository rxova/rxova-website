{
  "name": "@rxova/preview",
  "version": "0.0.8",
  "private": true,
  "type": "module",
  "description": "Renders @rxova/brand through the real consumer code path, so the theme can be looked at before it ships.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:astro": "astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.1",
    "@rxova/brand": "workspace:*",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "6.0.3"
  }
}
