---
'@rxova/astro-ui': minor
---

First release: the chrome (`SiteShell`, `Header`, `SiteFooter`, `ProjectSwitcher`, `ThemeToggle`, `ThemeScript`), the Starlight preset and overrides, the stylesheets and the primitives (`PageHeader`, `BackLink`, `ShowMore`, `VisuallyHidden`) that used to live in, or were copied around, `@rxova/brand`.
